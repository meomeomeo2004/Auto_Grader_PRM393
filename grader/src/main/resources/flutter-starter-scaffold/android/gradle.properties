org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Bien dich incremental cua Kotlin lam hong cache rieng cua no khi dung module Android
# cua plugin ("Could not close incremental caches ... class-fq-name-to-source.tab").
# Do that 22/9/2026 tren shared_preferences_android: loi TAT DINH, flutter clean va
# gradlew --stop deu khong cuu duoc vi lan dich sau lai tao cache moi roi hong y het.
# Khong rieng shared_preferences: moi plugin co ma Kotlin deu dinh (image_picker cung co).
# Bai khong dung plugin Kotlin nao thi dong nay nam im, khong ton gi.
kotlin.incremental=false
